import {StyleSheet} from 'react-native';

const styled = () => {
  return StyleSheet.create({
    profile: {
      marginLeft: 10,
      resizeMode: 'contain',
      width: 35,
      height: 35,
      borderRadius: 20,
    },
  });
};

export default styled;
