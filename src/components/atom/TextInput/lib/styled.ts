import {StyleSheet} from 'react-native';

const styled = StyleSheet.create({
  input: {
    padding: 6,
    fontSize: 12,
    height: 50,
    color: '#adadad',
    flex: 1,
  },
});

export default styled;
