import {StyleSheet} from 'react-native';

const styled = StyleSheet.create({
  input: {
    padding: 6,
    fontSize: 14,
    height: 50,
    color: '#adadad',
    flex: 1,
  },
});

export default styled;
