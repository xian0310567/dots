import {StyleSheet} from 'react-native';

const styled = StyleSheet.create({
  container: {
    borderRadius: 10,
    backgroundColor: '#333',
    overflow: 'hidden',
  },
});

export default styled;
