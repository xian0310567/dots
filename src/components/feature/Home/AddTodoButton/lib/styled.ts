import {StyleSheet} from 'react-native';

const styled = () => {
  return StyleSheet.create({
    button: {
      width: 30,
      display: 'flex',
      marginRight: 10,
    },
  });
};

export default styled;
