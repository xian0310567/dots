import {StyleSheet} from 'react-native';

const styled = () => {
  return StyleSheet.create({
    date: {
      color: '#fff',
      marginLeft: 10,
      fontSize: 14,
      fontWeight: '500',
    },
  });
};

export default styled;
