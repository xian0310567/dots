import {StyleSheet} from 'react-native';

const styled = () => {
  return StyleSheet.create({
    date: {
      color: '#fff',
      marginLeft: 10,
      fontSize: 16,
      fontWeight: '700',
    },
  });
};

export default styled;
